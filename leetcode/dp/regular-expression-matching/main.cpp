#include <iostream>
using namespace std;
class Solution {
    public:
         bool isMatch(string s, string p) {
            if (s.size() != p.size()) return false;
            return false;
         }
}